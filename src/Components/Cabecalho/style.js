import { StyleSheet,} from "react-native";

const style = StyleSheet.create({
    image: {
        width: 40,
        height: 40,
        borderRadius:30,
        marginRight:5
    },
    cabecalho:{
        flexDirection:"row",
        alignItems:"center",
        margin:10,
        
    }
});

export default style;